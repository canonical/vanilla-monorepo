import React from 'react';

interface {{name}}Props {
  // Add any props your component might have
}

const {{name}}: React.FC<{name}Props> = () => {
  return <div>{{name}} component</div>;
};

export default {{name}};
