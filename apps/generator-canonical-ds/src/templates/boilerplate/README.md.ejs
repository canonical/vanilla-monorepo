## <%= projectName %>

This application was generated by
[`<%= generatorPackageName %>`](<%= generatorPackageGitRepo %>) package version `<%= generatorPackageVersion %>`.

### Common commands

The following commands can be run from the root of the project with your package manager of choice (e.g. `npm run dev`).

| Command         | Description                                              |
|-----------------|----------------------------------------------------------|
| `dev`           | Run a live development server.                           |
| `build`         | Build the application for production.                    |
| `preview`       | Serve the production build locally.                      |
| `lint`          | Lint the codebase using Biome.                           |
| `format`        | Format the code.                                         |
| `check`         | Run both linting & type-checking.                        |
| `check:fix`     | Lint & resolve fixable issues.                           |
